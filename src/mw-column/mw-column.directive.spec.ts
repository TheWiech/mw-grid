import { MwColumnDirective } from './mw-column.directive';

describe('MwColumnDirective', () => {
    it('should create an instance', () => {
        const directive = new MwColumnDirective();
        expect(directive).toBeTruthy();
    });
});
